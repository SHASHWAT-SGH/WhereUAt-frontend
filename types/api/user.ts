export interface User {
  id: string; 
  firstName: string; 
  lastName: string;
  userEmail: string;
  imageUri: string;
}

export interface UserSearchedDTO {
  id: string; 
  firstName: string; 
  lastName: string;
  userEmail: string;
  profileImageUrl: string;
}